<nav class="navbar navbar-light navbar-expand-lg col-12 shadow-sm mb-4">
  <div class="container">
    <a href="<%= APP_ROOT %>/" class="navbar-brand logo">
      <img src="/images/FClogo.png">
      Support
    </a>
    <div class="collapse navbar-collapse">
      <ul class="navbar-nav mr-auto">
        <% if (locals.CURRENT_USER.roles.includes('operator')) { %>
        <li class="nav-item">
          <a class="nav-link" data-prefix="<%= APP_ROOT %>/rnipp" href="<%= APP_ROOT %>/rnipp">Redressement RNIPP</a>
        </li>
        <% } %>
        <li class="nav-item">
            <a class="nav-link" data-prefix="<%= APP_ROOT %>/account" href="<%= APP_ROOT %>/account">Comptes utilisateurs</a>
        </li>
      </ul>
      <div class="dropdown">
        <a class="dropdown-toggle mr-2" data-toggle="dropdown" href="#" role="button" aria-haspopup="true"
            aria-expanded="false"><i class="fa fa-git-square"></i></a>
        <div class="dropdown-menu dropdown-menu-right p-3 text-muted meta-information-container">
          <div class="meta-information">
            <i>Environnement : <%= APP_ENVIRONMENT %></i>
          </div>
          <div class="meta-information">
            <i>Branche : <%= GIT_CURRENT_BRANCH %></i>
          </div>
          <div class="meta-information">
            <i>Commit : <a
                href="<%= COMMIT_URL_PREFIX + GIT_LATEST_COMMIT_LONG_HASH %>"><%= GIT_LATEST_COMMIT_SHORT_HASH %></a></i>
          </div>
        </div>
      </div>
      <div class="dropdown">
        <a class="dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true"
            aria-expanded="false"><%= CURRENT_USER.username %></a>
        <div class="dropdown-menu dropdown-menu-right">
          <a class="dropdown-item" href="<%= APP_ROOT %>/logout">Déconnexion</a>
        </div>
      </div>
    </div>
  </div>
</nav>

<% if (messages.success) { %>
  <div class="row">
    <div class="col-10 offset-1 alert alert-success" role="alert">
      <button type="button" class="close" data-dismiss="alert" aria-label="Close">
        <span aria-hidden="true">x<span>
        <span class="sr-only">Fermer</span>
      </button>
      <%= messages.success[0] %>
    </div>
  </div>
<% } %>

<% if (locals.messages.errors && messages.errors[0]._totp) { %>
  <div class="row">
    <div class="col-10 offset-1 alert alert-danger" role="alert">
      <button type="button" class="close" data-dismiss="alert" aria-label="Close">
        <span aria-hidden="true">x<span>
        <span class="sr-only">Fermer</span>
      </button>
      <% messages.errors[0]._totp.forEach((error) => { %>
        <p> <%= error %></p>
      <% }) %>
    </div>
  </div>
<% } %>

