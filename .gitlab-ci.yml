stages:
  - build
  - test

global-install:
  image: node:8
  stage: build
  script:
    - npm ci
    - npm run lint
  artifacts:
    paths:
      - node_modules

include:
  - '/shared/.gitlab-ci.yml'
  - '/fc-exploitation/.gitlab-ci.yml'
  - '/fc-stats/.gitlab-ci.yml'
  - '/fc-workers/.gitlab-ci.yml'
