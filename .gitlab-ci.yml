stages:
  - build
  - test

lerna-bootstrap:
  image: node:8
  stage: build
  script:
    - npm ci
  artifacts:
    paths:
      - node_modules

include:
  - '/shared/.gitlab-ci.yml'
  - '/fc-stats/.gitlab-ci.yml'
